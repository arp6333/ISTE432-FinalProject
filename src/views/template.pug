// Ellie Parobek
// Template File to use for all other views
doctype html
html
  head
    title #{title}
    link(rel='stylesheet', href='/css/materialize.css')
    script(href='/js/materialize.js')
    meta(name="viewport" content="width=device-width, initial-scale=1")
  body
    main
      block header
        header.header
          h1 #{title}
      block content